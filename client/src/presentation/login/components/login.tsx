import { LoginComponent } from '../login_page';

import '../style/login.css';

export const LoginPage: React.FC = () => {
    return(
        <>
            <LoginComponent />
        </>
    );
}